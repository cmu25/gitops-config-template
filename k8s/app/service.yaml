apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}
  namespace: {{APP_NAME}}
spec:
  type: NodePort
  selector:
    app: {{APP_NAME}}
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080